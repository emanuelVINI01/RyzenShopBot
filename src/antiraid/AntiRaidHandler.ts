import { Channel } from "discord.js";
import { Message } from "discord.js/typings/index.js";

export default class AntiRaidHandler {
    public handleMessage(m : Message) : void {

    }
    public handleChannelDelete(c : Channel) {

    }
}